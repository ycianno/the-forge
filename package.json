{
  "name": "the-forge",
  "version": "1.0.0",
  "description": "The Forge — a self-hosted, single-user habit tracker that turns your goals into an RPG (XP, levels, attributes, quests, streaks, weekly boss).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "DB_PATH=/tmp/forge-dev.sqlite PORT=3099 node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "cookie-parser": "^1.4.7",
    "express": "^4.19.2",
    "web-push": "^3.6.7"
  }
}
